@*@using Ecard.Mvc.Models.GoodandOrder
@model EcardModelItem<CreateOrder>
@using (Html.BeginForm())
{  
    <ul>
        @Html.EditorFor(x => x.Item)
        <li class="buttons">
            <input type="submit" class="ui-state-default ui-corner-all" value="提交" />
            @Html.ActionLink("List")
        </li>
    </ul>
}*@
@model EcardModelItem<Ecard.Mvc.Models.GoodandOrder.CreateOrder>
@using (Html.BeginForm())
{  
     <div class="place">
    <span>位置：</span>
    <ul class="placeul">
    <li><a href="#">首页</a></li>
    <li><a href="#">@Model.Localize("title")</a></li>
    </ul>
</div>
    <div class="rightinfo">
   <ul class="forminfo" style="padding-top:10px;">
        @Html.EditorFor(x => x.Item)
    </ul> 
    <div class="submitDiv"> <input type="submit" class="scbtn" value="提交" />
            @Html.ActionLink("List")</div>
    </div>
    <script type="text/javascript">
        $(function () {
            $(".create_field input").focus(function () {
                $("[forprop='" + $(this).attr("id") + "']").show("1500");
            });
            $(".create_field select").click(function () {
                $("[forprop='" + $(this).attr("id") + "_Key']").show("1500");
            });
            $(".create_field input").blur(function () {
                $("[forprop='" + $(this).attr("id") + "']").hide("1500");
            });
            $(".create_field select").mouseleave(function () {
                $("[forprop='" + $(this).attr("id") + "_Key']").hide("1500");
            });
        });
    
    </script>
}